meta {
  name: 5. Delete FUT Task
  type: http
  seq: 5
}

delete {
  url: {{base-url}}/api/v1/futs/tasks/:id
  body: none
  auth: bearer
}

params:path {
  id: {{automationTaskId}}
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("status code is 200", function() {
      expect(res.getStatus()).to.equal(200);
  });
}
